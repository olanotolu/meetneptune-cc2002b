.venv/
venv/
__pycache__/
*.py[cod]
*.tmp
*.stage
.DS_Store
outputs/*.tmp
outputs/*.stage
# Regenerable presentation/bench artifacts — run the tools/ scripts to rebuild.
outputs/_demo/
outputs/_demo_combined/
outputs/_demo_step2/
outputs/_demo_determinism/
outputs/_bench/
# Local draft, not a deliverable.
EMAIL.md
.hypothesis/
.mypy_cache/
.ruff_cache/

# .python-version is committed on purpose: uv reads it to pick the interpreter.

# Signing keys never belong in the repo. The public half does — SIGNING_KEY.pub.
*.key
*.key.hex
